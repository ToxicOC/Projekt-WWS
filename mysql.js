/// datei für mysql stuff für connection un querys

var mysql = require('mysql');     // include für mysql node modul

var con = mysql.createConnection({
  host: "localhost",
  user: "yourusername",
  password: "yourpassword",
  database: "mydb"
});

con.connect(function(err) {
  if (err) throw err;
  console.log("Connected!");
});
